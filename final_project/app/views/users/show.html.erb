<header> <%= link_to 'logout', destroy_user_session_path, method: 'DELETE'%> </header>
<% if user_signed_in? %>
<h2> Welcome <%= current_user.name %>! </h2>

<% else %> 
<h2> no one logged in </h2>
<% end %> 

<h1> Profile for </h1>
<%= @user.name %>

