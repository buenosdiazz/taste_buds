 <% if user_signed_in? %>
  <% if current_user.follow?(list) %>
    <%= link_to "Unfollow", unfollow_path(list, "js"), method: :delete,  :remote => true  %> 
    <% else %>
    <%= link_to "follow", follow_path(list, "js"), method: :post,  :remote => true %>
    <% end %>
 <% else %>
      <%=  %>
 <% end %> 

